#include <stdio.h>
#include <conio.h>

void main()
{
    int a,b,c;
    printf("Enter two integer numbers:");
    scanf("%d%d",&a,&b);
    c=a+b;
    printf("The sum is:   %d.",c);
    getch();
    return 0;
}
